float 32 bit precision
double 64bit precision double precioon


constant numbers are doubles by default. Adding a f to the end changes it to a float

composition:
1 sign bit
8 exponent bits
32 fraction bits
( -1 ^ sign ) * (1.fraction) * ( 2 ^ (exponent - 127))
