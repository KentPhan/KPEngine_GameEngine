32BIT vs 64 Bit types


yo move by bytes use uint8_t instead of char for readability


POST Questions:
-inheritance and performance issues at the end of one of hte lectures
-Inlining constructors, destructors, functions in inheritance
-google stylesheet and not doing seperate files for inlining
-functions dont actually inline sometimes.
-Only takes place in release versions. Makes sense. Its like an optimization
-delete and inline
-do people do const pointers much? Your HeapManagerProxy didn't do it
-const integer parameters or primatives don't matter because they're copied right?
-same with copied objects
-pointers are copied when passed as an argument right?